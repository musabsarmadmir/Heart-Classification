APP_NAME=Heart Disease Predictor API
LOG_LEVEL=INFO

# Comma separated list of allowed web origins (update with your real Vercel domain after deploy)
CORS_ORIGINS=https://app.example.com,https://yourproject.vercel.app

# Paths inside the container (match Dockerfile copies)
MODEL_PATH=/app/models_saved/model.keras
FEATURE_ORDER_PATH=/app/models_saved/feature_order.json

# Optional (only if you need to reference dataset at runtime)
DATASET_PATH=/app/dataset/heart_cleveland_secondary.csv
